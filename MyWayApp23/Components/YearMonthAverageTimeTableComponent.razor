@*<MudTable Items="@Items" Dense="true" Hover="true" Bordered="true"
          Striped="true" Loading="IsVisible" RowsPerPage="31" FixedHeader="true" Height="400px">
    <ToolBarContent>

        <MudText>
            @HeaderText
        </MudText>

    </ToolBarContent>
    <HeaderContent>
        <MudTh Style="white-space: nowrap;">Mês</MudTh>
        <MudTh Style="white-space: nowrap;">Seg</MudTh>
        <MudTh Style="white-space: nowrap;">Ter</MudTh>
        <MudTh Style="white-space: nowrap;">Qua</MudTh>
        <MudTh Style="white-space: nowrap;">Qui</MudTh>
        <MudTh Style="white-space: nowrap;">Sex</MudTh>
        <MudTh Style="white-space: nowrap;">Sab</MudTh>
        <MudTh Style="white-space: nowrap;">Dom</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd Style="white-space: nowrap;" DataLabel="Mês">@context.Mes</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Seg">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Seg))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Ter">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Ter))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Qua">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Qua))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Qui">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Qui))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Sex">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Sex))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Sab">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Sab))</MudTd>
        <MudTd Style="white-space: nowrap;" DataLabel="Dom">@TimeOnly.FromTimeSpan(TimeSpan.FromMinutes(context.Dom))</MudTd>
    </RowTemplate>
</MudTable>

@code {
    [Parameter]
    public List<HistoricoAverageTime> Items { get; set; } = new List<HistoricoAverageTime>();
    [Parameter]
    public bool IsVisible { get; set; }
    [Parameter]
    public string HeaderText { get; set; } = string.Empty;
}
*@